version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: salon-postgres
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - salon-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Java Spring Boot Backend
  backend:
    build:
      context: ./Salon
      dockerfile: Dockerfile
    container_name: salon-backend
    environment:
      SERVER_SSL_ENABLED: "false"
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/postgres
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: 1234
      JWT_SECRET: MyVerySecureSecretKeyForJWTTokenGenerationThatIsAtLeast256BitsLong12345
      JWT_EXPIRATION: 86400000
      APP_PUBLIC_URL: http://localhost:8080
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - salon-network
    restart: unless-stopped

  # Blazor Frontend
  frontend:
    build:
      context: ./BlazorSalonApp
      dockerfile: BlazorSalonApp/Dockerfile
    container_name: salon-frontend
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8081
      ApiBaseUrl: http://backend:8080
    ports:
      - "8081:8081"
    depends_on:
      - backend
    networks:
      - salon-network
    restart: unless-stopped

networks:
  salon-network:
    driver: bridge

volumes:
  postgres_data:
